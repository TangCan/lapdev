# BMAD Method Configuration
# Lapdev Built-in Core Configuration

version: "1.0"
name: "BMAD Core"
description: "Built-in BMAD workflow for offline use"

workflows:
  default: "quick-flow"

agents:
  - developer
  - pm

settings:
  autoInstall: true
  fallbackMode: true
  skipUpdateCheck: false