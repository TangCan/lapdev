[package]
name = "lapdev-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "lapdev_core"
crate-type = ["cdylib"]

[dependencies]
globset = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

[profile.release]
lto = true